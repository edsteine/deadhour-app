
import 'package:flutter/material.dart';

class LocalExpertDashboardScreen extends StatelessWidget {
  const LocalExpertDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Local Expert Dashboard'),
      ),
      body: const Center(
        child: Text('Local Expert Dashboard Screen'),
      ),
    );
  }
}
