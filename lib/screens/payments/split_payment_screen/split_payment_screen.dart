
import 'package:flutter/material.dart';

class SplitPaymentScreen extends StatelessWidget {
  const SplitPaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Split Payment Screen'),
      ),
      body: const Center(
        child: Text('Split Payment Screen'),
      ),
    );
  }
}
